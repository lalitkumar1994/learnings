console.log("JS DSA");

// Object
// const obj = {
//   name: "lalit",
//   age: "28",
//   sayMyname: function () {
//     console.log(this.name);
//   }
// };
// .keys(),.values(), .entity()
// big O complexity
// access/remove/insert big O(1) constant
// search/ .keys/.values/.entity bigO(n) linear

// Array
// const a = [1, 2, 3, 4, 5];
// a.push(4); // add data to end of the array
// a.unshift(0); // add data to start of the array
// a.pop(); // remove data from end of the array
// a.shift(); // remove data from start of the array
// a.forEach((d) => console.log(d));

// map, forEach, reduce, filter, sort

// Big 0
// removing/insert from end of the array O(1)
// removing/insert from start of the array O(n )
// Access O(1)
// Search O(n)
// push/pop O(1)
// shift/unshift/splice/slice/concat O(n)
//  forEach/map/filter/reduce O(n)
